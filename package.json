{
	"name": "gitlab_repo_analyzer",
	"version": "1.0.0",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "next start",
		"build:cli": "npm run clean && esbuild src/index.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/index.js",
		"clean": "rm -rf dist/",
		"start:cli": "node dist/index.js",
		"dev:cli": "node --loader tsx/esm src/index.ts",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"all": "npm run lint && npm run typecheck && npm run test && npm run build",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:check": "drizzle-kit check"
	},
	"author": "",
	"license": "ISC",
	"description": "GitLabリポジトリを分析するNode.jsベースのツール",
	"devDependencies": {
		"@anthropic-ai/claude-code": "^1.0.43",
		"@biomejs/biome": "^2.0.0",
		"@types/node": "^22.16.3",
		"@types/react": "^18.0.0",
		"@types/react-dom": "^18.0.0",
		"esbuild": "^0.24.0",
		"tsx": "^4.19.2",
		"typescript": "^5.8.3",
		"vitest": "^2.0.0"
	},
	"dependencies": {
		"@types/pg": "^8.15.4",
		"axios": "^1.10.0",
		"dotenv": "^16.4.5",
		"drizzle-kit": "^0.31.4",
		"drizzle-orm": "^0.44.3",
		"next": "^15.0.0",
		"pg": "^8.16.3",
		"react": "^18.0.0",
		"react-dom": "^18.0.0",
		"zod": "^3.22.4"
	}
}
